'use client';

import { memo } from 'react';

const StoreInitialization = memo(() => {
  return null;
})

StoreInitialization.displayName = 'StoreInitialization';

export default StoreInitialization;
